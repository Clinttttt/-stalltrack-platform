// Live StallTrack API base. The admin console authenticates as the platform operator and drives
// activation against this backend. Override at build time if needed.
export const API_BASE_URL = 'https://api.stalltrack.site';
