/**
 * Where the portal's API lives.
 *
 * One address, deliberately: `payor.stalltrack.site` serves every municipality, and which office a payor belongs to
 * is decided by their own account rather than by the host they typed. The same base the operator console uses.
 */
export const API_BASE_URL = 'https://api.stalltrack.site';
